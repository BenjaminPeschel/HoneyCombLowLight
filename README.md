# HoneyCombLowLight
A full pipeline for removing the honey comb artifacts from nasal endoscopy and low light noise
# Notes
save_from_gpu, and save_from_cpu, both write into uncompressed, raw .avi files, which are huge. This was done deliberately to make the code easy to replicate. Nevertheless, I would recommend to change the codec, based on your individual needs
